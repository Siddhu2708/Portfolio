{
  "installCommand": "npm install --prefix client --include=dev",
  "buildCommand": "node scripts/copy-assets.js && npm run build --prefix client",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
